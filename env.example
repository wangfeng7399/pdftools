# ============================================
# PDF Summarizer - 环境变量配置示例
# ============================================
# 注意：以下变量在 .env.local 文件中已配置，部署到 Vercel 时直接复制即可：
# 
# 数据库相关变量（如果使用 Vercel Postgres，这些变量会由 Vercel 自动设置）：
# - DATABASE_URL (Vercel Postgres 会自动设置)
# - POSTGRES_URL (Vercel Postgres 会自动设置)
# - PRISMA_DATABASE_URL (如果使用 Prisma Accelerate，Vercel 会自动设置)
# - NEXT_PUBLIC_SUPABASE_URL
# - NEXT_PUBLIC_SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_ROLE_KEY
# - GOOGLE_CLIENT_ID
# - GOOGLE_CLIENT_SECRET
# - GITHUB_CLIENT_ID
# - GITHUB_CLIENT_SECRET
# - OPENROUTER_API_KEY
# - AUTH_SECRET
# - NEXTAUTH_URL
#
# 以下变量在 .env 中未配置，需要添加：

# ============================================
# 🔴 需要添加的必需环境变量
# ============================================

# 站点 URL（生产环境域名）
# 部署到 Vercel 时，替换为你的实际域名
NEXT_PUBLIC_SITE_URL="https://your-domain.vercel.app"


# ============================================
# 🟡 可选环境变量（根据需要添加）
# ============================================

# 清理任务密钥（用于保护 /api/admin/cleanup 端点）
# 生成方式：openssl rand -base64 32
CLEANUP_SECRET="your-secret-key"

# 服务器端站点 URL（可选，如果未设置会使用 NEXT_PUBLIC_SITE_URL）
SITE_URL="https://your-domain.vercel.app"

# Creem 支付配置（如果使用 Creem 作为支付提供商）
CREEM_API_BASE="https://api.creem.com/v1"
CREEM_API_KEY="your-creem-api-key"
CREEM_WEBHOOK_SECRET="your-webhook-secret"

# ============================================
# 数据库配置说明
# ============================================
# 
# 生产环境（Vercel）：
# - 使用 Vercel Postgres：在 Vercel Dashboard → Storage → Postgres 创建数据库
# - Vercel 会自动设置 DATABASE_URL、POSTGRES_URL 等环境变量
# - 无需手动配置数据库连接字符串
#
# 本地开发环境（SQLite）：
# - 本地开发时，可以使用 SQLite：
# DATABASE_URL="file:./dev.db"
