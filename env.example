# ============================================
# PDF Summarizer - 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填入实际值
# 部署到 Vercel 时，在 Vercel Dashboard → Settings → Environment Variables 中配置

# ============================================
# 🔴 必需环境变量
# ============================================

# 数据库配置（生产环境必须使用 PostgreSQL）
# Vercel Postgres: 在 Vercel Dashboard → Storage → Postgres 创建
# Supabase Postgres: 在 Supabase Dashboard → Database → Connection String 获取
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# Supabase 配置
# 获取方式：Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Google OAuth 配置
# 获取方式：Google Cloud Console → Credentials → OAuth 2.0 Client ID
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# GitHub OAuth 配置
# 获取方式：GitHub → Settings → Developer settings → OAuth Apps
GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# AI API 配置（OpenRouter）
# 获取方式：OpenRouter Dashboard → Keys
OPENROUTER_API_KEY="sk-or-v1-your-api-key"

# 站点 URL（生产环境域名）
NEXT_PUBLIC_SITE_URL="https://your-domain.vercel.app"

# ============================================
# 🟡 可选环境变量
# ============================================

# 清理任务密钥（用于保护 /api/admin/cleanup 端点）
# 生成方式：openssl rand -base64 32
CLEANUP_SECRET="your-secret-key"

# Creem 支付配置（如果使用 Creem 作为支付提供商）
CREEM_API_BASE="https://api.creem.com/v1"
CREEM_API_KEY="your-creem-api-key"
CREEM_WEBHOOK_SECRET="your-webhook-secret"

# 服务器端站点 URL（可选，如果未设置会使用 NEXT_PUBLIC_SITE_URL）
SITE_URL="https://your-domain.vercel.app"

# NextAuth URL（已迁移到 Supabase，但某些代码可能仍会使用）
NEXTAUTH_URL="https://your-domain.vercel.app"

# ============================================
# 本地开发环境（SQLite）
# ============================================
# 本地开发时，可以使用 SQLite：
# DATABASE_URL="file:./dev.db"
